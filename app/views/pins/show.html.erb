<%= link_to 'Back', pins_path %>

<div class="row">
	<div class="col-md-offset-2 col-md-8">
		<div class="panel panel-default">
			<div class="panel-heading center">
				<%= image_tag @pin.image.url, width: '400', default_url: "/app/assets/images/medium/missing.png" %>
			</div>
			<div class="panel-body">
			  <p><%= @pin.description %></p>
          <p><strong><%= @pin.user.email if @pin.user %></strong></p>
				<% if @pin.user == current_user %>
              <%= link_to edit_pin_path(@pin) do%>
                <span class="glyphicon glyphicon-edit"></span>
                Edit
              <% end %>
				<% end %>
			</div>
	</div>
</div>

<!--				<%= image_tag @pin.image.url(:medium, :default_url => "/app/assets/images/medium/missing.png") %> -->